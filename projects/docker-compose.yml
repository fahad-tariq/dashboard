services:
  projects:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ~/projects:/data/projects
      - ./data:/data/db
    environment:
      - PROJECTS_DIR=/data/projects
      - DB_PATH=/data/db/projects.db
